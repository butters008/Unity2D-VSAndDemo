using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrintTo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Super Cool Man!");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
